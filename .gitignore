# Binaries for programs and plugins
*.exe
*.exe~
*.dll
*.so
*.dylib

# Test binary, built with `go test -c`
*.test

# Output of the go coverage tool, specifically when used with LiteIDE
*.out

# go.sum should be committed
!go.sum

# CDK asset staging directory
.cdk.staging
cdk.out

# Remove vendor directory (use go mod tidy && go mod vendor for local development)
# Reasoning: AWS CDK is 5K files... gross
vendor/
aws/
awscliv2.zip
.env.development